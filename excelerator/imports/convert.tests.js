import chai from 'chai';

describe('Conversions', function () {
    it('can convert addresses to MB (sfWithin)', function () {
        // This code will be executed by the test driver when the app is started
        // in the correct mode
        chai.assert.equal(1,0);
    })
})